apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-infinity-datasource
  namespace: monitoring
  labels:
    grafana_datasource: "1"
data:
  infinity-datasource.yaml: |
    apiVersion: 1
    datasources:
      - name: yesoreyeram-infinity-datasource
        type: yesoreyeram-infinity-datasource
        uid: efhetakhtsvlsd
        access: proxy
        isDefault: false
        jsonData:
          allowedHosts:
            - http://192.168.122.230:8929
          httpHeaderName1: "PRIVATE-TOKEN"
        secureJsonData:
          httpHeaderValue1: "$__env{GITLAB_TOKEN}"
